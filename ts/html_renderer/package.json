{
  "name": "@cube-drone/marquee-html-renderer",
  "version": "0.1.0",
  "description": "Reference static HTML renderer for the Marquee markup language",
  "license": "MPL-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cube-drone/marqueemarkup.git",
    "directory": "ts/html_renderer"
  },
  "type": "module",
  "engines": {
    "node": ">=22.6.0"
  },
  "files": [
    "dist",
    "src"
  ],
  "publishConfig": {
    "access": "public"
  },
  "exports": {
    ".": {
      "marquee-src": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "test": "node --conditions=marquee-src --test test/*.test.ts",
    "check": "tsc --noEmit",
    "bless": "node --conditions=marquee-src scripts/bless.ts",
    "preview": "node --conditions=marquee-src scripts/preview.ts",
    "build": "tsc -p tsconfig.build.json",
    "prepack": "npm run build"
  },
  "dependencies": {
    "@cube-drone/marquee-parser": "^0.1.0"
  },
  "devDependencies": {
    "@cube-drone/marquee-css": "^0.1.0",
    "@cube-drone/marquee-fonts": "^0.1.0",
    "@cube-drone/marquee-turbolink": "^0.1.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
